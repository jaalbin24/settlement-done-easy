<h1>Settlement Index</h1>
<p>This is a list of all active settlements associated with <%=@organization.first_name%></p>

<table class="table table-sm table-hover table-borderless mx-auto w-50">
    <thead class="border-bottom border-secondary">
        <tr>
            <th>Status</th>
            <th>Attorney</th>
            <th>Agent</th>
            <th>Amount</th>
        </tr>
    </thead>
    <tbody>
        <%@organization.settlements.each do |s|%>
            <tr class="table-secondary">
                <td><%=render "status", settlement: s%></td>
                <td><%=link_to s.attorney.full_name, settlement_show_path(s)%></td>
                <td><%=link_to s.insurance_agent.full_name, settlement_show_path(s)%></td>
                <td>$<%=s.settlement_amount%></td>
            </tr>
        <%end%>
    </tbody>
</table>